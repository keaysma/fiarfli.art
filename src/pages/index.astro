---
// Component Imports
import Hero from "/src/components/hero/Skitty.vue";
import Art from "/src/components/art/BlockGrid.vue";
import Commissions from "/src/components/commissions/Basic.vue";
import ContactMe from "/src/components/contact-me/Basic.vue";
import MadeWithLove from "/src/components/footer/made-with-love.vue";

// Application State
import { about, commissions, contact, contactFormEnabled } from "/src/components/content.json";
import { blocks } from "/src/components/index.json";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Raquel Bumgarner</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <style>
      @font-face {
        font-family: "Raquel";
        src: url(/fonts/Raquelfont_01.ttf);
      }

      @font-face {
        font-family: "Nanum Pen Script";
        src: url(/fonts/nanum-pen-script-v19-latin-regular.woff2);
      }

      :root {
        --light-blue: 173, 216, 230;
      }

      html,
      body {
        width: 100vw;
        overflow-x: hidden;
        overflow-y: scroll;

        font-family: "Nanum Pen Script";
        font-size: 36px;
        line-height: 1.25em;
        letter-spacing: -1px;
        margin: 0;
        padding: 0;

        scroll-behavior: smooth;
      }

      a {
        /*text-decoration: none;*/
        color: white;
      }
      main {
        position: relative;
        display: block;
        width: 100vw;

        padding-top: 5em;
        background: linear-gradient(180deg, transparent 0, rgba(var(--light-blue), 0.125) 1.5em, rgba(var(--light-blue), 1) 16em, pink 48em, #fffee1);
      }
    </style>
  </head>
  <body>
    <Hero client:load {about} {blocks} commissionsEnabled={commissions.enabled} />
    <main>
      <Art client:visible {blocks} />
      <Commissions {commissions} />
      <ContactMe client:load {contact} {contactFormEnabled} />
      <MadeWithLove/>
    </main>
  </body>
</html>
